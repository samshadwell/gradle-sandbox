def setupProject = {
    apply plugin: 'java'

    dependencies {
        implementation platform(project(':platform'))
    }
}

gradle.beforeProject {
    if (it.project != rootProject && !it.project.path.startsWith(":platform")) {
        it.with(setupProject)
    }
}